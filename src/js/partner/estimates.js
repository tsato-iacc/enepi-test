/**
 * Controller_Callings
 */
if ($('.area-estimates').length) {
  
  // See helper_functions.js
  cancelEstimateOrContact('estimate', 'partner');

  /**
   * page parner/estimates/:id
   */
  // setUpUnitPrice();
}
