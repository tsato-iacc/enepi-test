/**
 * Enable popovers everywhere
 */
$('[data-toggle="popover"]').popover();

/**
 * Datepicker
 */
$('.datepicker').datepicker({
    format: "yyyy-mm-dd",
    todayHighlight: true,
    autoclose: true,
    language: 'ja'
});
